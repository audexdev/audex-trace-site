# Audex Trace Site

This repository contains the standalone GitHub Pages landing page for Audex Trace, a macOS menu bar utility for real-time playback format detection and automatic output format switching.

The site is plain static HTML, CSS, and JavaScript with no framework or build step. It is intended to be served from the repository root on the `main` branch.
